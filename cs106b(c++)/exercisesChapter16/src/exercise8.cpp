#include "exercise8.h"
#include <iostream>
#include "console.h"
#include "simpio.h"

/*
struct BSTNode {
    string key;
    BSTNode *left, *right;
};

void hasBinarySearchProperty(BSTNode *tree, bool &hasBinarySearchProperty) {
    if (tree->left != NULL) hasBinarySearchProperty = tree->key >= tree->left->key;
    if (tree->right != NULL) hasBinarySearchProperty = tree->key <= tree->right->key;
    hasBinarySearchProperty(tree->left,hasBinarySearchProperty);
    hasBinarySearchProperty(tree->right,hasBinarySearchProperty);
}

bool hasBinarySearchProperty(BSTNode *tree) {

    bool hasBinarySearchProperty = true;
    hasBinarySearchProperty(tree, hasBinarySearchProperty);
    return hasBinarySearchProperty;
}
*/

//using namespace std;

void exercise8() {
  //we need to implement func hasBinarySearchProperty

}
