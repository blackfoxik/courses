#include "exercise7.h"
#include <iostream>
#include "console.h"
#include "simpio.h"

/*
struct BSTNode {
    string key;
    BSTNode *left, *right;
};

int isBalanced(BSTNode *tree, bool &isBalanced) {
    int leftST = 0;
    int rightST = 0;
    if (tree->left != NULL) {
        leftST = height(tree->left);
    }
    if (tree->right != NULL) {
        rightST = tree->right;
    }
    if ((leftST - rightST == -2) || (rightST - leftST == -2)) isBalanced = false;
    if (leftST >= rightST) return leftST + 1;
    return rightST + 1;
}

bool isBalanced(BSTNode *tree) {
    bool isBalanced = true;
    isBalanced(tree, isBalanced);
    return isBalanced;
}
*/
//using namespace std;

void exercise7() {
  //we need to implement func isBalaced

}
